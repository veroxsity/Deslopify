services:
  deslopify:
    image: veroxsity/deslopify:latest
    container_name: deslopify
    ports:
      - "7381:7381"
    environment:
      - DESLOPIFY_TRANSPORT=sse
      - DESLOPIFY_PORT=7381
      - DESLOPIFY_STRICTNESS=medium
    restart: unless-stopped
