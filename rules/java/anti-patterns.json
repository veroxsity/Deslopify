{
  "anti_patterns": [
    {
      "id": "JV_AP001",
      "name": "string_concatenation_loop_java",
      "severity": "warning",
      "category": "performance",
      "description": "String concatenation with + in a loop creates a new String object each iteration. Use StringBuilder.",
      "detection": "Detect string += inside for/while loops.",
      "fix": "Use StringBuilder sb = new StringBuilder(); sb.append(...); in the loop."
    },
    {
      "id": "JV_AP002",
      "name": "raw_types",
      "severity": "warning",
      "category": "robustness",
      "description": "Using raw types (List instead of List<String>) bypasses generics and loses compile-time type checking.",
      "detection": "Detect raw Collection types without type parameters.",
      "fix": "Always parameterize collections: List<String>, Map<Integer, User>, etc."
    },
    {
      "id": "JV_AP003",
      "name": "catching_throwable",
      "severity": "error",
      "category": "robustness",
      "description": "Catching Throwable or Error catches JVM-level problems like OutOfMemoryError. Only catch Exception or specific subtypes.",
      "detection": "Detect catch(Throwable) or catch(Error) blocks.",
      "fix": "Catch Exception or specific exception types. Never catch Throwable or Error."
    },
    {
      "id": "JV_AP004",
      "name": "system_exit_in_library",
      "severity": "error",
      "category": "architecture",
      "description": "System.exit() should never be called in library code. It kills the entire JVM. Throw an exception instead.",
      "detection": "Detect System.exit() calls.",
      "fix": "Throw an appropriate exception. Let the top-level caller decide whether to exit."
    },
    {
      "id": "JV_AP005",
      "name": "synchronized_method",
      "severity": "warning",
      "category": "performance",
      "description": "Synchronizing an entire method is usually too coarse-grained. Only the critical section needs protection.",
      "detection": "Detect public synchronized methods.",
      "fix": "Use synchronized blocks on specific objects. Consider java.util.concurrent locks for finer control."
    },
    {
      "id": "JV_AP006",
      "name": "string_equals_order",
      "severity": "warning",
      "category": "robustness",
      "description": "variable.equals(\"literal\") throws NullPointerException if variable is null. Reverse the order.",
      "detection": "Detect variable.equals(\"string literal\") patterns.",
      "fix": "Use \"literal\".equals(variable) or Objects.equals(variable, \"literal\")."
    },
    {
      "id": "JV_AP007",
      "name": "concatenation_in_logger",
      "severity": "info",
      "category": "performance",
      "description": "String concatenation in logger calls evaluates even when the log level is disabled. Use parameterized messages.",
      "detection": "Detect logger.info(\"msg\" + var) or log.debug(\"msg\" + var) patterns.",
      "fix": "Use logger.info(\"Message: {}\", var) with SLF4J parameterized logging."
    },
    {
      "id": "JV_AP008",
      "name": "field_injection",
      "severity": "warning",
      "category": "testability",
      "description": "@Autowired/@Inject on fields makes classes hard to test and hides dependencies. Use constructor injection.",
      "detection": "Detect @Autowired or @Inject on private/protected fields.",
      "fix": "Use constructor injection: private final Service service; public MyClass(Service service) { this.service = service; }"
    }
  ]
}
